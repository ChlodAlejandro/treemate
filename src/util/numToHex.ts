/**
 * Converts any number to a hexadecimal number.
 *
 * @param number The number to convert.
 * @returns The number in hexadecimal.
 */
export default function(number : number) : string {
    return number.toString(16);
}